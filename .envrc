# Direnv configuration for Cursor package development
export NIXPKGS_ALLOW_UNFREE=1

# You can also add this to your flake like this instead:
# In your flake.nix outputs:
# pkgs = import nixpkgs {
#   inherit system;
#   config.allowUnfree = true;
# };
